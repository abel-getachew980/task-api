# Used when running the API locally (outside Docker) against the DB container
DATABASE_URL="postgresql://taskuser:taskpass@localhost:5432/taskdb"
PORT=3000
JWT_SECRET="change-me-in-local-dev"
JWT_EXPIRES_IN="1d"
AUTH_EMAIL="admin@task.local"
AUTH_PASSWORD="password123"
AUTH_NAME="Task Admin"
